Copy Makefile together with the SPA directory to the directory desired to download sources and compile the SPA.
Run make in that directory.
Run SPA by running run.sh from a created build/tools/clang/tools/SPA directory.
